# Day 43: React.memo & React Profiler

## 키워드

- React.memo
- **React Profiler** — 컴포넌트 렌더 횟수·소요 시간을 측정하는 도구. memo/useMemo/useCallback 적용 "전후"를 근거로 판단할 때 사용.

## 면접 포인트

(작성 예정)

## 목표

- React.memo가 언제 효과가 있고 언제 무의미한지 구분한다.
- **React Profiler로 측정한 근거**를 바탕으로 최적화 여부를 결정한다(추측 금지).
