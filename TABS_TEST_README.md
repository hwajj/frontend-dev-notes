# 탭 전환 기능 테스트 프로젝트

## 🚀 실행 방법

1. **의존성 설치**

```bash
npm install
```

2. **개발 서버 실행**

```bash
npm run dev
```

3. **브라우저에서 확인**

- 자동으로 `http://localhost:5173/tabs-test.html`이 열립니다
- 또는 수동으로 접속하세요

## 📁 파일 구조

```
src/
  ├── components/
  │   └── Tabs.tsx          # 탭 컴포넌트 (20~30줄)
  ├── App.tsx               # 메인 앱 컴포넌트
  └── main.tsx              # React 엔트리 포인트
```

## ✅ 요구사항 체크리스트

- [x] 탭 3개: "홈", "프로필", "설정"
- [x] 클릭하면 해당 탭의 내용이 바뀐다
- [x] 현재 선택된 탭은 굵게 표시한다
- [x] 컴포넌트 전체는 1개의 파일에서 구현 (20~30줄 목표)

## 🎯 학습 포인트

1. **상태 관리**: `activeTab` 상태를 최상단에서 관리
2. **Props 흐름**: Tabs → TabList → TabButton
3. **조건부 렌더링**: `activeTab` 값에 따라 다른 내용 표시

## 📝 다음 단계 제안

- 파일 분리 버전 만들기
- `useReducer`로 상태 관리 변경
- 탭 내용에 API 페칭 추가
