# 💰 동적 계획법 (Dynamic Programming, DP)

> 주제: 동적 계획법
> 목표: 각 알고리즘의 대표 문제를 예시로 개념, 입력·출력, 코드, 포인트까지 한눈에 보기

---

## 📍 핵심 개념 요약

| 개념            | 설명                                                                 |
| --------------- | -------------------------------------------------------------------- |
| **동적 계획법** | 큰 문제를 작은 문제로 나누어 저장하고 재활용하는 알고리즘            |
| **핵심 원칙**   | 점화식(이전 상태 → 현재 상태) 세우기                                 |
| **최적화 기법** | 중복 계산 방지(메모이제이션, Tabulation)                             |
| **활용 분야**   | 피보나치 수열, 계단 오르기, 최대 부분합, 동전 거스름돈, 배낭 문제 등 |

---

## 📘 **문제 1: LeetCode 70. Climbing Stairs**

> n개의 계단을 오르는 방법의 수를 구하라. (한 번에 1개 또는 2개씩 오를 수 있음)

### 💬 **입출력 예시**

| 입력    | 출력 |
| ------- | ---- |
| `n = 2` | `2`  |
| `n = 3` | `3`  |
| `n = 5` | `8`  |

### 💻 **코드 + 주석**

```js
// (예시 문제)
// - LeetCode 70. Climbing Stairs
// - LeetCode 198. House Robber
// - LeetCode 746. Min Cost Climbing Stairs
// - LeetCode 213. House Robber II
// - LeetCode 53. Maximum Subarray
function climbStairs(n) {
  let dp = [1, 1];
  for (let i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
  }
  return dp[n];
}
```

**핵심 포인트:**

- `dp[i] = dp[i-1] + dp[i-2]` 점화식 사용
- i번째 계단에 도달하는 방법 = (i-1)번째에서 1칸 오르기 + (i-2)번째에서 2칸 오르기
- 피보나치 수열과 동일한 구조

---

## 📘 **문제 2: LeetCode 198. House Robber**

> 인접한 집을 털 수 없을 때, 훔칠 수 있는 최대 금액을 구하라.

### 💬 **입출력 예시**

| 입력                 | 출력 |
| -------------------- | ---- |
| `nums = [2,7,9,3,1]` | `12` |
| `nums = [1,2,3,1]`   | `4`  |

### 💻 **코드 + 주석**

```js
// House Robber
function rob(nums) {
  if (nums.length === 0) return 0;
  if (nums.length === 1) return nums[0];

  let dp = [nums[0], Math.max(nums[0], nums[1])];

  for (let i = 2; i < nums.length; i++) {
    dp[i] = Math.max(dp[i - 1], dp[i - 2] + nums[i]);
  }

  return dp[nums.length - 1];
}
```

**핵심 포인트:**

- `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`
- i번째 집을 털지 않거나(dp[i-1]), 털거나(dp[i-2] + nums[i]) 중 최댓값 선택

---

## 📘 **문제 3: LeetCode 322. Coin Change**

> 주어진 동전으로 특정 금액을 만드는 최소 동전 개수를 구하라.

### 💬 **입출력 예시**

| 입력                           | 출력 |
| ------------------------------ | ---- |
| `coins = [1,2,5], amount = 11` | `3`  |
| `coins = [2], amount = 3`      | `-1` |

### 💻 **코드 + 주석**

```js
// (예시 문제)
// - LeetCode 322. Coin Change
// - LeetCode 300. Longest Increasing Subsequence
// - LeetCode 1143. Longest Common Subsequence
// - LeetCode 139. Word Break
// - LeetCode 518. Coin Change 2
function coinChange(coins, amount) {
  let dp = Array(amount + 1).fill(Infinity);
  dp[0] = 0;

  for (let coin of coins) {
    for (let i = coin; i <= amount; i++) {
      dp[i] = Math.min(dp[i], dp[i - coin] + 1);
    }
  }

  return dp[amount] === Infinity ? -1 : dp[amount];
}
```

**핵심 포인트:**

- `dp[i] = min(dp[i], dp[i-coin] + 1)` - 각 동전을 사용했을 때 최소 개수 갱신
- 1D 배열로 최적화 (이전 상태만 필요)
- 그리디로 풀 수 없는 경우(동전 단위가 배수 관계가 아닐 때) DP 사용

---

## 📘 **문제 4: LeetCode 53. Maximum Subarray**

> 연속된 부분 배열의 최대 합을 구하라.

### 💬 **입출력 예시**

| 입력                             | 출력 |
| -------------------------------- | ---- |
| `nums = [-2,1,-3,4,-1,2,1,-5,4]` | `6`  |
| `nums = [1]`                     | `1`  |

### 💻 **코드 + 주석**

```js
// Maximum Subarray (Kadane's Algorithm)
function maxSubArray(nums) {
  let dp = nums[0];
  let maxSum = nums[0];

  for (let i = 1; i < nums.length; i++) {
    // 이전까지의 합이 음수면 버리고, 현재부터 다시 시작
    dp = Math.max(nums[i], dp + nums[i]);
    maxSum = Math.max(maxSum, dp);
  }

  return maxSum;
}
```

**핵심 포인트:**

- `dp[i] = max(nums[i], dp[i-1] + nums[i])`
- 이전까지의 합이 음수면 버리고 현재 원소부터 다시 시작
- 공간 최적화: 배열 대신 변수 하나만 사용 가능

---

## ⚡ **핵심 포인트**

| 구분       | 내용                                                         |
| ---------- | ------------------------------------------------------------ |
| 시간복잡도 | 대부분 O(n) 또는 O(n²)                                       |
| 공간복잡도 | O(n) 또는 O(1) (최적화 시)                                   |
| 핵심 패턴  | 점화식 세우기, 중복 계산 방지                                |
| 응용       | 피보나치, 계단 오르기, 동전 거스름돈, 배낭 문제, LCS, LIS 등 |

---

## 📝 스터디 문제 정리

<!-- 스터디에서 제공된 문제를 여기에 추가하세요 -->

---
