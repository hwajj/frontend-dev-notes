# 🔗 위상정렬 (Topological Sort)

> 주제: 위상정렬
> 목표: 각 알고리즘의 대표 문제를 예시로 개념, 입력·출력, 코드, 포인트까지 한눈에 보기

---

## 📍 핵심 개념 요약

| 개념        | 설명                                       |
| --------- | ---------------------------------------- |
| **위상정렬**   | 방향 그래프에서 모든 간선이 왼쪽에서 오른쪽으로 향하도록 정렬하는 알고리즘          |
| **전제 조건**  | DAG (Directed Acyclic Graph) - 사이클이 없는 방향 그래프             |
| **핵심 아이디어**    | 진입 차수(in-degree)가 0인 노드부터 처리        |
| **활용 분야** | 작업 스케줄링, 의존성 해결, 코스 수강 순서, 빌드 시스템 등 |

---

## 📘 **문제: LeetCode 207. Course Schedule**

> 코스 수강 순서 문제 (위상정렬)

### 💬 **입출력 예시**

| 입력                    | 출력    |
| --------------------- | ----- |
| `numCourses = 2, prerequisites = [[1,0]]` | `true` |
| `numCourses = 2, prerequisites = [[1,0],[0,1]]` | `false` |

### 💻 **코드 + 주석**

```js
// 위상정렬을 사용한 코스 수강 가능 여부 판단
function canFinish(numCourses, prerequisites) {
  // 1. 그래프와 진입 차수 배열 초기화
  let graph = Array.from({ length: numCourses }, () => []);
  let inDegree = Array(numCourses).fill(0);
  
  for (let [course, prereq] of prerequisites) {
    graph[prereq].push(course);
    inDegree[course]++;
  }
  
  // 2. 진입 차수가 0인 노드들을 큐에 추가
  let queue = [];
  for (let i = 0; i < numCourses; i++) {
    if (inDegree[i] === 0) queue.push(i);
  }
  
  // 3. BFS로 위상정렬 수행
  let count = 0;
  while (queue.length) {
    let node = queue.shift();
    count++;
    
    for (let neighbor of graph[node]) {
      inDegree[neighbor]--;
      if (inDegree[neighbor] === 0) {
        queue.push(neighbor);
      }
    }
  }
  
  // 4. 모든 노드를 방문했으면 사이클 없음 (true)
  return count === numCourses;
}
```

**핵심 포인트:**
- 진입 차수가 0인 노드부터 처리
- 인접 노드의 진입 차수를 감소시키며 처리
- 모든 노드를 방문하지 못하면 사이클 존재 (false)

---

## ⚡ **핵심 포인트**

| 구분    | 내용                                          |
| ----- | ------------------------------------------- |
| 시간복잡도 | O(V + E)                                    |
| 자료구조  | 큐 (BFS 사용)                                    |
| 전제 조건  | DAG (사이클 없음)        |
| 응용    | 작업 스케줄링, 의존성 해결, 코스 수강 순서, 빌드 시스템 등 |

---

## 📝 스터디 문제 정리

<!-- 스터디에서 제공된 문제를 여기에 추가하세요 -->

---

